@model TrashCollector2.Models.Pickup

@{
    ViewBag.Title = "Map View";
}

<h2>@Model.Address, @Model.City, @Model.State, @Model.ZipCode</h2>
<br />

<meta name="viewport" content="initial-scale=1.0, user-scalable=no">
<meta charset="utf-8">
<style>
    #map {
        height: 400px;
        width: 100%;
    }
</style>

@{if (Model == null) {
    @Html.TextBox("address", value: "", htmlAttributes: new { style = "width: 100%;" })
    }
    else
    {
    @Html.TextBox("address", value: Model.Address + " " + Model.City + " " + Model.State + " " + Model.ZipCode, htmlAttributes: new { style = "width: 100%;" })
    }
}
<input id="submit" type="button" value="Locate">

<div class="row">
    <br />
    <div id="map"></div>
    <script>
              function initMap() {
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 8,
          center: { lat: 42.717915, lng: -87.840409 }
        });
        var geocoder = new google.maps.Geocoder();

        document.getElementById('submit').addEventListener('click', function() {
          geocodeAddress(geocoder, map);
        });
      }

      function geocodeAddress(geocoder, resultsMap) {
          var address = document.getElementById('address').value;

        geocoder.geocode({'address': address}, function(results, status) {
          if (status === 'OK') {
              resultsMap.setCenter(results[0].geometry.location);

            var marker = new google.maps.Marker({
                map: resultsMap,
                position: results[0].geometry.location
            });
          }
          else {
            alert('Location not Found: ' + status);
          }
        });
      }
    </script>
    <script async defer
            src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDGQzRBHuUI2g2c2zUytzl0E2JPMy2IJwo&callback=initMap">
    </script>
</div>